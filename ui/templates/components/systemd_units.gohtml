{{ define "systemd-unit" }}
<div class="service-card">
	<!-- Service info -->
	<div class="content-area">
		<div class="player-name mb-1">
			{{ if eq .Open "panel" }}
			<a href="#"
			   onclick="openServicePanel('{{ .URL }}', '{{ if .Description }}{{ .Description }}{{ else }}{{ .Name }}{{ end }}'); return false;"
			   title="{{ .URL }}"
			   class="hover:underline">{{ if .Description }}{{ .Description }}{{ else }}{{ .Name }}{{ end }} <span class="inline-flex align-middle opacity-60 [&_svg]:h-3.5 [&_svg]:w-3.5"><span class="hidden sm:inline">{{ template "icon-panel-right" }}</span><span class="sm:hidden">{{ template "icon-panel-bottom" }}</span></span></a>
			{{ else if eq .Open "self" }}
			<a href="#"
			   onclick="openServiceHere('{{ .URL }}'); return false;"
			   title="{{ .URL }}"
			   class="hover:underline">{{ if .Description }}{{ .Description }}{{ else }}{{ .Name }}{{ end }}</a>
			{{ else if .URL }}
			<a href="#"
			   onclick="openServiceUrl('{{ .URL }}'); return false;"
			   title="{{ .URL }}"
			   class="hover:underline">{{ if .Description }}{{ .Description }}{{ else }}{{ .Name }}{{ end }} <span class="text-xs opacity-60">↗</span></a>
			{{ else }}
			{{ if .Description }}{{ .Description }}{{ else }}{{ .Name }}{{ end }}
			{{ end }}
		</div>
		<div class="flex items-center gap-2">
			<div class="player-meta-small">{{ .Name }}</div>
			{{ if .IsUser }}
			<button class="hover:scale-110 transition-transform [&_svg]:h-4 [&_svg]:w-4"
			        hx-post="/services/user/{{ .Name }}/restart"
			        hx-swap="none"
			        title="Restart service">{{ template "icon-rotate-cw" }}</button>
			{{ if .Active }}
			<button class="hover:scale-110 transition-transform [&_svg]:h-4 [&_svg]:w-4"
			        hx-post="/services/user/{{ .Name }}/stop"
			        hx-swap="none"
			        title="Stop service">{{ template "icon-ban" }}</button>
			{{ end }}
			{{ end }}
		</div>
	</div>

	<!-- Status indicator -->
	<div class="meta-area">
		<span class="flex items-center gap-1.5 text-xs">
			<span class="status-dot {{ if .Active }}bg-lime{{ else }}bg-red-500{{ end }}"></span>
			<span class="{{ if .Active }}text-lime{{ else }}text-red-400{{ end }}">
				{{ .State }}
			</span>
		</span>
	</div>
</div>
{{ end }}
